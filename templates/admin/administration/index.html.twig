{% extends 'base.html.twig' %}

{% block title %}Page Administration{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <section>
        <div>
{#            <a href="{{ path('app_admin_user_edit') }}" class="btn btn-outline-dark flex-shrink-0">Gestion des utilisateurs</a>#}
{#            <a href="{{ path('app_admin_author_new') }}" class="btn btn-outline-dark flex-shrink-0">Ajouter un auteur</a>#}
{#            <a href="{{ path('app_admin_author_edit') }}" class="btn btn-outline-dark flex-shrink-0">Modifier un auteur</a>#}
{#            <a href="{{ path('app_admin_editor_new') }}" class="btn btn-outline-dark flex-shrink-0">Ajouter un editeur</a>#}
{#            <a href="{{ path('app_admin_editor_edit') }}" class="btn btn-outline-dark flex-shrink-0">Ajouter un editeur</a>#}
            <div>page d'administration</div>
        </div>
    </section>
{% endblock %}
